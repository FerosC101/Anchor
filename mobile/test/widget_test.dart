import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — integration tests live in the integration_test/ directory.
  testWidgets('app smoke test', (tester) async {
    expect(true, isTrue);
  });
}
